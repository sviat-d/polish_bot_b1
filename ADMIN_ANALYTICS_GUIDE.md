# Руководство по аналитике и администрированию

## 📊 Собираемые данные о пользователях

### Автоматический сбор из Telegram

С каждым взаимодействием бот автоматически собирает и обновляет:

```javascript
{
  chatId: 123456789,                    // Telegram Chat ID
  username: "john_doe",                 // @username (если есть)
  firstName: "John",                    // Имя пользователя
  lastName: "Doe",                      // Фамилия (если есть)
  languageCode: "ru",                   // Язык Telegram клиента
  isBot: false,                         // Бот или человек

  createdAt: "2026-01-18T10:00:00Z",   // Дата регистрации
  updatedAt: "2026-01-18T15:30:00Z",   // Последнее обновление

  completedTasks: ["task_001", ...],   // ID выполненных заданий
  answers: [                            // История ответов
    {
      taskId: "task_001",
      topic: "Rekcja czasowników",
      isCorrect: true,
      userAnswer: "C",
      rating: 5,                         // Оценка задания (1-5)
      answeredAt: "2026-01-18T10:05:00Z"
    }
  ],

  language: "ru",                       // Предпочитаемый язык объяснений
  ratingEnabled: false,                 // Включена ли оценка заданий
  weakTopicMode: {                      // Режим тренировки слабой темы
    active: false,
    topic: null
  }
}
```

### Где хранятся данные?

**Railway Volume**: `/app/data/users/`

Каждый пользователь = отдельный JSON файл:
```
data/users/
  ├── 123456789.json
  ├── 987654321.json
  └── ...
```

## 🔐 Настройка доступа администратора

### 1. Узнай свой Chat ID

Отправь `/start` боту и посмотри в логи Railway или используй бот [@userinfobot](https://t.me/userinfobot)

### 2. Добавь в Railway Environment Variables

1. Открой Railway → твой проект → **Settings** → **Variables**
2. Добавь переменную:
   - **Name**: `ADMIN_CHAT_IDS`
   - **Value**: `твой_chat_id` (например: `123456789`)

Для нескольких админов используй запятую:
```
ADMIN_CHAT_IDS=123456789,987654321,555555555
```

### 3. Редеплой

Railway автоматически перезапустит бот с новыми правами.

## 📱 Команды администратора в боте

После настройки доступа используй эти команды в Telegram:

### `/admin`
Показывает общую статистику:
- Количество пользователей (всего, новые, активные)
- Статистика ответов (правильные/неправильные, точность)
- Средний прогресс
- Предпочтения языка
- Рейтинг

### `/admin top`
Топ-10 пользователей по количеству выполненных заданий:
- 🥇🥈🥉 Места
- Имя/username
- Количество заданий
- Точность

### `/admin recent`
Новые пользователи за последние 7 дней:
- Имя/username
- Дата регистрации
- Количество выполненных заданий

### `/admin dates`
График регистраций по дням (последние 14 дней):
```
2026-01-18: ████████ 8
2026-01-17: ████ 4
2026-01-16: ██ 2
```

### `/admin help`
Справка по админским командам

## 💻 Аналитика через CLI (на сервере)

### Подключение к Railway

```bash
# Установить Railway CLI
npm install -g @railway/cli

# Войти
railway login

# Подключиться к проекту
railway link

# Запустить команду
railway run node scripts/analytics.js
```

### Использование analytics.js

#### Базовая статистика
```bash
node scripts/analytics.js
```

Показывает:
- Общая статистика
- Топ-10 пользователей
- Новые пользователи (7 дней)

#### Подробная статистика
```bash
node scripts/analytics.js --detailed
```

Добавляет:
- График регистраций по дням
- Распределение по выполненным заданиям

#### Список всех пользователей
```bash
node scripts/analytics.js --users
```

Таблица всех пользователей с:
- Chat ID
- Username
- Имя
- Количество заданий
- Количество ответов
- Язык

#### Топ пользователей
```bash
node scripts/analytics.js --top
```

#### Новые пользователи
```bash
node scripts/analytics.js --recent
```

#### Экспорт в JSON
```bash
node scripts/analytics.js --export
```

Создаёт файл `users-export.json` с данными всех пользователей для дальнейшего анализа.

### Пример вывода

```
============================================================
📊  POLISH B1 EXAM BOT — ANALYTICS
============================================================

📈 OVERALL STATISTICS:
   Total Users:        127
   New Users (0 ans):  23
   Active Users:       104
   Total Answers:      3421
   Correct:            2847 (83%)
   Incorrect:          574
   Avg Progress:       26 tasks

🌐 LANGUAGE PREFERENCE:
   Russian (RU):       98 users
   Polish (PL):        29 users

⭐ RATING FEATURE:
   Enabled:            45 users

📅 NEW USERS BY DATE (last 7 days):
   2026-01-18:         8 users
   2026-01-17:         12 users
   2026-01-16:         5 users
   ...

✅ TASK COMPLETION DISTRIBUTION:
   0-9        ████ 23
   10-19      ████████ 35
   20-29      ██████ 28
   30-39      ████ 19
   40-49      ██ 12
   50-59      █ 7
   60-70      █ 3

🏆 TOP 10 USERS (by completed tasks):
   Rank | Chat ID    | Username      | Tasks | Accuracy | Lang
   ------------------------------------------------------------
      1 | 123456789  | john_doe      |    68 |     92%  | ru
      2 | 987654321  | anna_pol      |    64 |     88%  | pl
      3 | 555555555  | learner123    |    57 |     85%  | ru
   ...
```

## 📊 Отчёты и метрики

### Ключевые метрики

| Метрика | Описание | Как считается |
|---------|----------|----------------|
| **Total Users** | Всего зарегистрированных | Количество файлов в `data/users/` |
| **New Users** | Пользователи без ответов | `answers.length === 0` |
| **Active Users** | Пользователи с ответами | `answers.length > 0` |
| **Accuracy Rate** | Общая точность | `correct / total * 100%` |
| **Avg Progress** | Средний прогресс | `sum(completedTasks) / totalUsers` |

### Аналитика по темам

Для каждого пользователя считается:
- Процент ошибок по каждой теме
- Слабые темы (error rate ≥ 50% при ≥ 5 попытках)

### Engagement метрики

- **Rating adoption**: % пользователей с включённым рейтингом
- **Completion rate**: % заданий, выполненных активными пользователями
- **Retention**: распределение по количеству выполненных заданий

## 🔍 Использование данных

### Найти пользователя по username

```bash
railway run bash
cd data/users
grep -l '"username":"john_doe"' *.json
cat 123456789.json | jq
```

### Найти всех пользователей с языком PL

```bash
railway run node << 'EOF'
const fs = require('fs');
const files = fs.readdirSync('data/users');
files.forEach(f => {
  const user = JSON.parse(fs.readFileSync(`data/users/${f}`));
  if (user.language === 'pl') {
    console.log(`${user.chatId}: ${user.firstName} (@${user.username})`);
  }
});
EOF
```

### Экспорт для Google Sheets / Excel

1. Запусти:
   ```bash
   node scripts/analytics.js --export
   ```

2. Скачай `users-export.json`

3. Импортируй в Google Sheets или конвертируй в CSV:
   ```bash
   npm install -g json2csv
   json2csv -i users-export.json -o users.csv
   ```

## 🚨 Проблемы и решения

### "No users found" в analytics

**Причина**: Railway Volume не подключён или пуст

**Решение**:
1. Проверь, что Volume подключён: Railway → Settings → Volumes
2. Проверь Mount Path: `/app/data/users`
3. Проверь, что хотя бы один пользователь запустил бот

### Username = null у многих пользователей

**Причина**: Не все пользователи Telegram устанавливают @username

**Решение**: Используй `firstName` и `chatId` для идентификации

### Старые пользователи без username/firstName

**Причина**: Были созданы до обновления кода

**Решение**: При следующем взаимодействии данные обновятся автоматически

## 📈 Рекомендации по аналитике

### Ежедневный мониторинг

1. `/admin` в Telegram — общая статистика
2. Проверь accuracy rate (должен быть 70-85%)
3. Проверь новых пользователей

### Еженедельный анализ

1. `node scripts/analytics.js --detailed`
2. Проверь топ пользователей
3. Проанализируй retention (распределение по заданиям)
4. Экспортируй данные для глубокого анализа

### Ежемесячный отчёт

1. Экспортируй всех пользователей в JSON
2. Посчитай:
   - MoM growth (рост месяц к месяцу)
   - DAU/MAU (если есть логи активности)
   - Retention cohorts
3. Анализируй слабые темы (топ ошибок)

## 🔮 Будущие улучшения

Возможные расширения аналитики:

- [ ] Dashboard с графиками (React + Chart.js)
- [ ] Webhook для отправки ежедневных отчётов
- [ ] Интеграция с Google Analytics
- [ ] A/B тесты заданий
- [ ] Cohort analysis
- [ ] Funnel analysis (регистрация → первое задание → 10 заданий → 50 заданий)
