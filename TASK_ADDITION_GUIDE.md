# Руководство по добавлению заданий в Polish B1 Exam Bot

## Процесс добавления новых заданий

### Шаг 1: Подготовка данных

1. Получить задания в структурированном формате:
   - ID задания (например, `B1_LICZEBNIKI_001`)
   - Вопрос (QUESTION)
   - 4 варианта ответа (OPTIONS: A, B, C, D)
   - Правильный ответ (CORRECT_ANSWER)
   - Объяснение на русском (EXPLANATION_RU)
   - Объяснение на польском (EXPLANATION_PL)
   - Тема (GRAMMAR_TOPIC)
   - Сложность (DIFFICULTY)

### Шаг 2: Форматирование объяснений

**Важно**: Удалить весь Markdown форматирование (** для жирного текста), потому что Telegram не парсит Markdown автоматически.

✅ **Разрешено**:
- Эмодзи: 🔑 ❌ ⚠️ ✅ 💡
- Простой текст
- Переносы строк

❌ **Запрещено**:
- `**жирный текст**` (будет показываться как `**текст**`)
- Другие Markdown символы

### Шаг 3: Парсинг и преобразование

```python
import re
import json

# 1. Извлечь задания из исходного формата
# 2. Удалить ** из объяснений:
explanation_ru = re.sub(r'\*\*([^*]+)\*\*', r'\1', explanation_ru)
explanation_pl = re.sub(r'\*\*([^*]+)\*\*', r'\1', explanation_pl)

# 3. Заменить фигурные кавычки на прямые:
content = content.replace('"', '"').replace('"', '"')
content = content.replace(''', "'").replace(''', "'")
```

### Шаг 4: Добавление в pdfTasks.js

```javascript
// В конце файла src/data/pdfTasks.js, ПЕРЕД ];

  // TASK 71
  {
    id: "B1_NEW_TOPIC_001",
    source: "pdf",
    question: "Twoje pytanie tutaj?",
    options: [
      { label: "A", text: "Opcja A" },
      { label: "B", text: "Opcja B" },
      { label: "C", text: "Opcja C" },
      { label: "D", text: "Opcja D" }
    ],
    correctAnswer: "A",
    explanationRu: `Объяснение на русском...`,
    explanationPl: `Wyjaśnienie po polsku...`,
    topic: "Новая тема",
    difficulty: "medium"
  }
```

### Шаг 5: Регенерация tasks.json

```bash
node -e "const loader = require('./src/data/taskLoader'); const tasks = loader.getTasks(); const fs = require('fs'); fs.writeFileSync('./data/tasks.json', JSON.stringify(tasks, null, 2)); console.log('Regenerated tasks.json with', tasks.length, 'tasks');"
```

### Шаг 6: Коммит и деплой

```bash
git add src/data/pdfTasks.js data/tasks.json
git commit -m "feat: add X new tasks covering [topics]"
git push -u origin claude/polish-exam-bot-tT77d
```

## Важные замечания

### ✅ ПРАВИЛЬНО:
- Добавлять задания в КОНЕЦ массива (перед `];`)
- Использовать уникальные ID для новых заданий
- Проверять грамматику польского и русского
- Удалять Markdown форматирование
- Заменять фигурные кавычки на прямые

### ❌ НЕПРАВИЛЬНО:
- НЕ изменять ID существующих заданий
- НЕ удалять старые задания без миграции
- НЕ использовать Markdown в объяснениях
- НЕ забывать регенерировать tasks.json

## Типичные ошибки

### 1. SyntaxError: Unexpected identifier
**Причина**: Фигурные кавычки в строках
**Решение**: Заменить " " на " и ' ' на '

### 2. Задания не появляются в боте
**Причина**: Забыли регенерировать tasks.json
**Решение**: Запустить команду регенерации (Шаг 5)

### 3. Эмодзи не отображаются
**Причина**: Проблема с кодировкой файла
**Решение**: Использовать UTF-8 кодировку

## Чеклист перед коммитом

- [ ] Все новые задания имеют уникальные ID
- [ ] Удалён весь Markdown (** и т.д.)
- [ ] Заменены фигурные кавычки на прямые
- [ ] Проверена грамматика польского и русского
- [ ] Регенерирован tasks.json
- [ ] Проверено количество заданий (должно совпадать)
- [ ] Коммит с понятным сообщением
